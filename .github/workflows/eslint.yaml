name: eslint
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: |
      cd frontend
      npm install
    - name: Run ESLint
      run: |
      cd frontend
      eslint . --ext .js,.jsx,.ts,.tsx